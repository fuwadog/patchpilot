"""PatchPilot CLI command handlers.

Each submodule exposes simple functions (not classes) that accept the
relevant managers and produce output via the Rich ``output`` module.
"""

from __future__ import annotations
